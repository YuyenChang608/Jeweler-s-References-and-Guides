
export interface GaugeInfo {
  bnsGauge: number;
  millimeters: number;
  inches: string;
  sawBlade: string;
  drillSize: string;
}

export interface RingSizeInfo {
  usSize: number;
  insideDiameter: number; // in mm
}
